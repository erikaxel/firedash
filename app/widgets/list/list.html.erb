<h1 class="title">{{title}}</h1>

<ol>
    <li ng-repeat="item in data.items">
        <span class="label" data-bind="item.label">{{item.label}}</span>
        <span class="value" data-bind="item.value">{{item.value}}</span>
    </li>
</ol>

<ul class="list-nostyle">
    <li ng-repeat="item in data.items">
        <span class="label" data-bind="item.label">{{item.label}}</span>
        <span class="value" data-bind="item.value">{{item.value}}</span>
    </li>
</ul>

<%= render partial: '/shared/footer' %>
